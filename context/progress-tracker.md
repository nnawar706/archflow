# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Feature 16: Edge Behavior — complete

## Current Goal

- Start Feature 17 (`context/features/17-canvas-ergonomics.md.md`).

## Completed

- **01 — Design System**: `shadcn/ui` initialized (Nova preset, Lucide icons, CSS variables). Added Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea to `components/ui/`. Installed `lucide-react`. `lib/utils.ts` created with `cn()`. `app/globals.css` rewritten with the dark-only palette from `context/ui-context.md`: raw tokens (`--bg-base`, `--text-primary`, `--accent-primary`, etc.) plus a `@theme inline` mapping to both shadcn's standard tokens (`--background`, `--primary`, `--border`, ...) and project-specific Tailwind utilities (`bg-base`, `bg-surface`, `text-copy-primary`, `text-copy-muted`, `border-surface-border`, `text-brand`, `bg-accent-dim`, etc.). `<html>` in `app/layout.tsx` carries a permanent `dark` class (no light mode toggle). Verified via `tsc --noEmit` and `npm run build`.
- **02 — Editor Chrome**: Added `components/editor-navbar.tsx` — fixed-height (`h-14`) top navbar with left/center/right sections; left holds a sidebar toggle `Button` swapping `PanelLeftOpen`/`PanelLeftClose` off an `isSidebarOpen` prop; `onToggleSidebar` callback passed in; dark `bg-surface` background with `border-b border-surface-border`. Added `components/sidebar.tsx` — `isOpen`/`onClose`-controlled overlay sidebar, `absolute` positioned (floats over the canvas, does not push layout) and slides in via `translate-x` transition; header with "Projects" title + close button; shadcn `Tabs` for "My Projects" / "Shared", both rendering an empty placeholder state; full-width "New Project" button with `Plus` icon pinned to the bottom. Added `components/base-dialog.tsx` — reusable dialog pattern wrapping shadcn `Dialog` with `title`/`description`/`footer`/`children` props, styled `rounded-3xl` per the modal radius scale; no concrete dialogs built on top of it yet (that's later feature work). All three components are client components (event handlers, Radix `Tabs`/`Dialog`) and live flat in `components/`. Added `components/editor-shell.tsx` — client component that owns the `isSidebarOpen` state and composes `EditorNavbar` + `ProjectSidebar` + a `children` slot for canvas content, wiring the navbar toggle and the sidebar's close button to the same state. Verified via `tsc --noEmit`, `npm run lint`, and `npm run build`. *(`editor-shell.tsx` was later removed in Feature 04 — see below — and its logic now lives directly in `app/editor/page.tsx`.)*
- **03 — Auth**: Installed `@clerk/ui` for the theme package (Core 3 SDK; `@clerk/nextjs` was already present). Added `proxy.ts` at the project root (Next.js 16 renamed `middleware.ts` → `proxy.ts`) using `clerkMiddleware` + `createRouteMatcher` in a protected-first pattern: everything is protected by `auth.protect()` except `NEXT_PUBLIC_CLERK_SIGN_IN_URL`/`NEXT_PUBLIC_CLERK_SIGN_UP_URL` (added to `.env.local`/`.env.example` as `/sign-in` and `/sign-up`, since no sign-in/sign-up env vars previously existed in the repo). `app/layout.tsx` wraps `{children}` in `ClerkProvider` inside `<body>` (Core 3 requirement — provider cannot wrap `<html>`), themed with `dark` from `@clerk/ui/themes` and an `appearance.variables` object that maps every color to the app's existing CSS custom properties (`var(--accent-primary)`, `var(--bg-elevated)`, `var(--border-default)`, etc. — no hardcoded colors) plus `borderRadius: "var(--radius)"`. Added `app/sign-in/[[...sign-in]]/page.tsx` and `app/sign-up/[[...sign-up]]/page.tsx` (catch-all routes, required by Clerk for internal sub-steps like SSO callback/verification), both wrapped in a new `components/auth-layout.tsx` — a server component with a 50/50 two-panel layout on large screens (left panel: `bg-surface` for a gray-ish differentiation from the `bg-base` right side, logo, headline, short paragraph, and a plain text-only feature list with no icons/cards/gradients per spec; right panel: centered `<SignIn>`/`<SignUp>` capped at `max-w-sm` to prevent overflow) and form-only on small screens (`hidden lg:flex` on the left panel). `app/page.tsx` is now a pure `redirect("/editor")` — unauthenticated visitors never reach it because `proxy.ts` redirects them to `/sign-in` first; the canvas placeholder that used to live at `/` moved to the new `app/editor/page.tsx`. Added Clerk's `<UserButton />` to the right section of `components/editor-navbar.tsx`. Verified via `tsc --noEmit`, `npm run lint`, `npm run build`, and headless-browser screenshots of `/sign-in` and `/sign-up` at desktop (1600px, confirming 50/50 split + gray left panel) and mobile (430px, confirming no horizontal overflow via a Puppeteer layout inspection — `.cl-card` measured 392px wide inside a 430px viewport).
- **04 — Project Dialogs**: Added `types/project.ts` (`Project { id, name, slug, ownerId }`) and a `slugify()` helper in `lib/utils.ts`. Added `hooks/use-project-dialogs.ts` — the dedicated hook the spec asked for, owning dialog state (`activeDialog: "create" | "rename" | "delete" | null`), form state (`nameInput`, `slugPreview` derived via `useMemo`), loading state (`isLoading`, with a simulated 300ms delay per mutation), and an in-memory mock project list (three seeded projects: two owned by `user_mock_owner`, one owned by a different mock user to exercise the shared/owned split) with local create/rename/delete mutators — no API calls, per spec. The hook exposes a single `submit()` dispatcher that routes to the right mutator based on `activeDialog`, rather than exposing three separate submit functions, so callers don't need mode-switch logic of their own. Added `components/project-dialog.tsx` — **one** reusable dialog (not three) built on `components/base-dialog.tsx`; it takes a `mode: "create" | "rename" | "delete" | null` prop and switches its title/description/body/footer accordingly (name input + live slug preview for create, prefilled+auto-focused input for rename, no input + `variant="destructive"` confirm for delete). The create/rename inputs live in an internal `<form id="project-dialog-form">` so Enter submits and the `BaseDialog` footer's submit button (outside the form in the DOM) still triggers it via `form="project-dialog-form"`. Fixed `components/base-dialog.tsx`'s `DialogTitle` to explicitly render `text-lg font-semibold text-copy-primary` (previously unstyled, which was rendering as black/low-contrast in some cases; now explicitly white-ish and a step larger than the shadcn default `text-base`). Added `components/editor-home.tsx` — plain (no Card wrapper) centered heading/description/`New Project` button for the `/editor` home state. Rewrote `components/sidebar.tsx` to accept `ownedProjects`/`sharedProjects`/`onCreate`/`onRename`/`onDelete` props: each row shows rename/delete icon buttons (`Pencil`/`Trash2`) revealed on hover (`group-hover`) or focus (`group-focus-within`) only when `canManage` (My Projects tab); the Shared tab never renders them. Added a mobile-only backdrop scrim (`absolute inset-0 z-30 bg-black/50 lg:hidden`, `onClick={onClose}`) rendered behind the sidebar (`z-40`) whenever it's open, so tapping outside closes it below the `lg` breakpoint only — desktop is unaffected. `components/editor-shell.tsx` was removed: `app/editor/page.tsx` is now itself a `"use client"` page (it needs `useState`/the dialogs hook, so it can no longer be a Server Component) that directly composes `EditorNavbar` + `ProjectSidebar` + `EditorHome` + the single `ProjectDialog`, since this composition was specific to the `/editor` route rather than a reusable component. Verified via `tsc --noEmit`, `npm run lint`, and `npm run build` (all clean) after both the initial implementation and this refactor. Did not verify visually in-browser: `/editor` sits behind Clerk auth (`proxy.ts`) and the user opted to eyeball it themselves rather than have the CLI script an impersonation session against their real Clerk account for a screenshot.
- **05 — Prisma**: Added `prisma/models/project.prisma` (multi-file schema — `prisma/schema.prisma` keeps only `generator`/`datasource`) with `ProjectStatus` enum (`DRAFT`/`ARCHIVED`, defaulted to `DRAFT`), `Project` (`id` cuid PK, `ownerId`, `name`, optional `description`, `status`, optional `canvasJsonPath`, `createdAt`/`updatedAt`, indexes on `ownerId` and `createdAt`), and `ProjectCollaborator` (`id` cuid PK, `projectId` FK with `onDelete: Cascade`, `email`, `createdAt`, unique on `[projectId, email]`, indexes on `email` and `[projectId, createdAt]`). The generator uses the newer ESM `"prisma-client"` provider (already configured pre-feature) outputting to `app/generated/prisma` (gitignored); its entry point is `app/generated/prisma/client.ts`, not an index file, so `lib/prisma.ts` imports from `@/app/generated/prisma/client`. Added `lib/prisma.ts`: a `global`-cached singleton (dev-only caching, matching the Next.js hot-reload pattern) whose factory branches on whether `DATABASE_URL` starts with `prisma+postgres://` — that branch calls `new PrismaClient()` with no adapter (Accelerate is not installed/wired yet, just left as the no-adapter default per spec's dependency list); otherwise it builds `new PrismaPg({ connectionString: databaseUrl })` from `@prisma/adapter-pg` and passes it as `adapter`. Fixed `prisma.config.ts` (Prisma-generated boilerplate) to load env vars explicitly via `dotenv`'s `config({ path })` for both `.env` and `.env.local` (the latter with `override: true`) — its original `import "dotenv/config"` only read `.env`, but the real `DATABASE_URL` lives in `.env.local` per this repo's existing convention (Feature 03), so `prisma migrate`/`generate` couldn't find it until this was fixed. Ran `npx prisma migrate dev --name init_project_models` against the project's real pooled Postgres (`DATABASE_URL` in `.env.local`, a direct `postgres://...@pooled.db.prisma.io` connection string, not `prisma+postgres://` — so this project currently exercises the adapter-pg branch, not Accelerate) — migration applied cleanly and `prisma generate` ran automatically after. Verified via `tsc --noEmit`, `npm run lint`, and `npm run build` (all clean).
- **06 — Project APIs**: Added `app/api/projects/route.ts` (`GET` lists the authenticated user's projects via `prisma.project.findMany({ where: { ownerId: userId }, orderBy: { createdAt: "desc" } })`; `POST` creates one, defaulting `name` to `"Untitled Project"` when missing/blank, `id` left to the schema's `cuid()` default — no sequential ID added) and `app/api/projects/[projectId]/route.ts` (`PATCH` renames, `DELETE` deletes; both call a local `findOwnedProject(projectId, userId)` helper that 404s if the project doesn't exist and 403s if `project.ownerId !== userId` before mutating). Every handler starts with `const { userId } = await auth()` from `@clerk/nextjs/server` and returns 401 via a shared `errorResponse(401, "Unauthorized")` if absent — enforced explicitly in each route rather than relied on from `proxy.ts`/`auth.protect()`, since that middleware-level protection redirects/404s browser-style and doesn't guarantee a JSON 401 for fetch callers. Added `lib/project-api.ts`: a single `errorResponse(status: number, message: string)` helper (`NextResponse.json({ error: message }, { status })`) used for every 401/403/404/400 case, plus `parseProjectName(body: unknown): string | null`, which safely extracts and trims a `name` field from an unknown JSON body (returns `null` if absent, non-string, or blank after trim) — used by both `POST` (falls back to `"Untitled Project"`) and `PATCH` (missing/blank name is a `400`, since renaming requires an explicit name unlike creation). Request bodies are parsed via `request.json().catch(() => null)` so a missing/empty body doesn't throw. Dynamic route params follow Next.js 16's async convention (`{ params }: { params: Promise<{ projectId: string }> }`, awaited before use). `Project` type for the local helper is imported from `@/app/generated/prisma/client` (the aliased `Prisma.ProjectModel` export), not `.../models`, since the barrel-exported model type there is `ProjectModel`, not `Project`. UI was not touched, per spec. Verified via `tsc --noEmit`, `npm run lint`, and `npm run build` (all clean; route table confirms `ƒ /api/projects` and `ƒ /api/projects/[projectId]` are registered as dynamic).
- **08 — Editor Workspace Shell**: Added `app/editor/[roomId]/page.tsx` — an `async` Server Component that calls the new `lib/project-access.ts` (`getCurrentIdentity()`: wraps `auth()` + `currentUser()` into a `{ userId, email } | null`; `hasProjectAccess(project, identity)`: pure predicate, true if `project.ownerId === identity.userId` or `identity.email` matches a row in `project.collaborators`; `getAccessibleProject(projectId, identity)`: fetches the project with `collaborators` included and returns it only if `hasProjectAccess` passes, else `null`) — redirects to `/sign-in` if unauthenticated, renders the new `components/access-denied.tsx` (centered lock icon, message, link back to `/editor`) if the project doesn't exist or access fails, otherwise fetches sidebar data via the existing `getOwnedAndSharedProjects` (Feature 07) and renders the new `components/workspace-shell.tsx`. Added, flat in `components/` (spec asked for `access-denied.tsx` under a `components/editor/` path, but an initial attempt at a per-route subfolder was flattened back out to match this repo's existing flat-`components/` convention): `workspace-shell.tsx` (`"use client"`, owns `isSidebarOpen`/`isAiSidebarOpen` state, reuses `hooks/use-project-actions.ts` and `components/project-dialog.tsx` exactly as `editor-shell.tsx` does), `canvas-placeholder.tsx` (server component, dark centered placeholder message — no canvas logic per scope), and `ai-sidebar-placeholder.tsx` (`"use client"`, right-side slide-over overlay mirroring `ProjectSidebar`'s absolute-positioned pattern but anchored `right-0`/`translate-x-full`, toggled from the navbar — this is layout-shell state only, not real AI chat, which stays out of scope). Extended `components/editor-navbar.tsx` with optional props (`projectName`, `showWorkspaceActions`, `isAiSidebarOpen`, `onToggleAiSidebar`) rather than a separate navbar component, so `/editor` (home) and `/editor/[roomId]` (workspace) share one navbar; the Share button renders only visually (no `onClick`) since sharing behavior is explicitly out of scope, while the AI toggle is functionally wired since it only opens/closes the placeholder panel. Extended `components/sidebar.tsx` with an optional `activeProjectId` prop threaded down to `ProjectRow`, which now renders a filled brand-colored dot + tinted row (`bg-accent-dim`/`text-brand`) when `project.id === activeProjectId` — unused (undefined) on the `/editor` home route, so no visual change there. Verified via `tsc --noEmit`, `npm run lint`, and `npm run build` (all clean; route table now shows `ƒ /editor/[roomId]` as a dynamic server route alongside `ƒ /editor`).
- **07 — Wire Editor Home**: `app/editor/page.tsx` is now an `async` Server Component again (previously a `"use client"` page since Feature 04) — it calls `auth()` (redirecting to `/sign-in` if unauthenticated, defensively; `proxy.ts` already guarantees this) and `currentUser()` for the primary email, then fetches both project lists via the new `getOwnedAndSharedProjects(userId, email)` helper in `lib/projects.ts` before rendering. No client-side fetch for initial load, per spec. All client state (sidebar open/close, dialogs) moved into a new `components/editor-shell.tsx` (`"use client"`, takes `ownedProjects`/`sharedProjects` as props) — this reintroduces the shell component that Feature 04 had folded into the page; splitting it back out was required once the page itself needed to be a Server Component for the data fetch. `lib/projects.ts`'s `getOwnedAndSharedProjects` runs the owned query (`ownerId: userId`) and the shared query (`ProjectCollaborator` rows matching the user's email, `ownerId: { not: userId }`) in parallel via `Promise.all`; the shared query is skipped (`[]`) when Clerk has no primary email. `types/project.ts` now re-exports Prisma's generated `Project` type directly (`export type { Project } from "@/app/generated/prisma/client"`) instead of the old hand-rolled `{ id, name, slug, ownerId }` shape — the real schema has no `slug` field, so the mock shape was wrong once real data replaced it. Replaced `hooks/use-project-dialogs.ts` (deleted, in-memory mock) with `hooks/use-project-actions.ts` — the hook name and mutation responsibilities the spec asked for. **Create**: generates a short random suffix (`Math.random().toString(36).slice(2, 8)`) appended to `slugify(name)` to form a room ID (e.g. `checkout-service-a1b2c3`), sends it as `id` in the `POST /api/projects` body, then `router.push`es straight to `/editor/${roomId}` — this makes the Liveblocks room ID and the project's database ID the same value by construction, per spec ("project ID and Liveblocks room ID should stay aligned"), rather than generating them separately and reconciling later. This required extending `POST /api/projects` (Feature 06) to accept an optional client-supplied `id`: `lib/project-api.ts` gained `parseOptionalProjectId(body)`, validating against `/^[a-z0-9](?:[a-z0-9-]{0,60}[a-z0-9])?$/` and returning a discriminated `{ ok: true, id? } | { ok: false }` so the route can tell "no id sent" (fall back to the schema's `cuid()` default) apart from "id sent but malformed" (`400`). **Rename**/**Delete**: call `PATCH`/`DELETE /api/projects/[id]` respectively; both read/write a lightweight `targetProject: { id, name }` (not the full `Project` object) since that's all the dialogs need. Rename calls `router.refresh()` on success (re-runs the Server Component fetch — no separate client list state to reconcile). Delete compares the deleted project's id against `useParams<{ roomId?: string }>().roomId` (the still-unbuilt `/editor/[roomId]` route from Feature 08) to decide `router.push("/editor")` vs `router.refresh()` — on the current `/editor` home route this param is always `undefined`, so delete always refreshes there today; the active-workspace redirect only engages once Feature 08 ships the dynamic route, and no changes will be needed here when it does. All three mutations go through a local `requestJson()` helper that throws on a non-`ok` response using the JSON body's `error` field; the hook catches this into a new `error: string | null` field, which `components/project-dialog.tsx` now optionally renders (`error?: string | null` prop, shown as `text-error` text below the dialog body) — dialog failures (e.g. a 403 on a stale project) no longer fail silently. `components/sidebar.tsx` and `components/project-dialog.tsx` needed no other changes — their prop shapes were already generic enough for the real hook, matching the plan noted after Feature 04. Verified via `tsc --noEmit`, `npm run lint`, and `npm run build` (all clean; `/editor` now shows as `ƒ` dynamic in the route table, confirming it renders server-side per request rather than being statically prerendered).
- **09 — Share Dialog**: Wired the previously visual-only navbar `Share` button (Feature 08) to a real dialog. `lib/project-access.ts` gained `requireProjectOwner(projectId, userId)` — mirrors the existing local `findOwnedProject` pattern from `app/api/projects/[projectId]/route.ts` (404 if missing, 403 if not owner) but promoted to `lib/` since two new route files now need it, matching the promotion rule already noted below from Feature 06. Added `lib/collaborator-api.ts` (sibling to `lib/project-api.ts`, same file-split convention) with `parseEmail(body)` — trims/lowercases and validates against a basic `/^[^\s@]+@[^\s@]+\.[^\s@]+$/` pattern. Added `lib/collaborators.ts` with `enrichCollaborators(collaborators: ProjectCollaborator[])`, which batch-fetches Clerk users via `(await clerkClient()).users.getUserList({ emailAddress: [...] })` (from `@clerk/nextjs/server` — no separate `@clerk/backend` install needed, it's already the underlying dependency) and maps each collaborator row to `{ id, email, name, avatarUrl }`, falling back to `name: null`/`avatarUrl: null` (email-only) when no Clerk user matches. Added `app/api/projects/[projectId]/collaborators/route.ts`: `GET` (any user with project access — owner or existing collaborator, via `hasProjectAccess`) returns `{ collaborators, isOwner }`; `POST` (owner-only, via `requireProjectOwner`) validates the email, creates a `ProjectCollaborator` row, and catches Prisma's `P2002` unique-constraint error (`projectId`+`email`) to return `409 "This email is already a collaborator"` instead of a raw 500. Added `app/api/projects/[projectId]/collaborators/[collaboratorId]/route.ts`: `DELETE` (owner-only) 404s if the collaborator doesn't belong to that project before deleting. Added `hooks/use-share-dialog.ts` — owns all dialog state (`isOpen`, fetched `collaborators`/`isOwner`, invite form, per-row `removingId`, `error`, `copied`) and exposes `openDialog()`/`closeDialog()` rather than reacting to an `open` prop via `useEffect`: the first version used a fetch-on-open `useEffect`, but `eslint-plugin-react-hooks`'s new `set-state-in-effect` rule (bundled with `eslint-config-next` 16, part of the React Compiler lint rules) flags *any* synchronous `setState` call inside a `useEffect` body — including plain "reset form state" branches — as a "cascading renders" risk. Restructured so the dialog's own `openDialog()` is called directly from the navbar's `onClick` (an event handler, not an effect), matching the existing `use-project-actions.ts` convention (`openCreateDialog`, etc.) and avoiding `useEffect` entirely. Added `components/share-dialog.tsx` — purely presentational (all state/handlers passed as props, same split as `ProjectDialog`), built on `BaseDialog`: a "Copy project link" button (`navigator.clipboard.writeText`, `Copied!` label for 2s), an email `Input` + `Invite` button shown only when `isOwner`, and a scrollable collaborator list (Clerk avatar `<img>` or an email-initial fallback badge, name+email or email-only, a per-row remove `X` button shown only when `isOwner`). `components/editor-navbar.tsx` gained an optional `onShare` prop wired to the Share `Button`'s `onClick`. `components/workspace-shell.tsx` owns the single `useShareDialog(project.id)` instance (alongside its existing `useProjectActions()` instance) and passes everything down to `<ShareDialog>` as props. Verified via `tsc --noEmit`, `npm run lint` (clean aside from a pre-existing-style `@next/next/no-img-element` warning — no `next.config.ts` `remotePatterns` configured for Clerk's avatar CDN yet, so a plain `<img>` was used rather than adding unrelated config), and `npm run build` (all clean; route table shows both new `ƒ /api/projects/[projectId]/collaborators` routes as dynamic). Not verified visually in-browser, same reasoning as Feature 04/08 — the flow sits behind Clerk auth and needs a second collaborator account to exercise the read-only path.
- **10 — Liveblocks Setup**: Added `@liveblocks/node` (pinned to `3.23.1`, matching the already-installed `@liveblocks/client`/`@liveblocks/react`/`@liveblocks/react-flow`/`@liveblocks/react-ui` versions — the spec's "all required packages are already installed" was inaccurate for the server SDK, so it was installed as part of this feature). Added `liveblocks.config.ts` at the project root (module augmentation of the global `Liveblocks` interface, per the standard Liveblocks codegen shape): `Presence` = `{ cursor: {x,y} | null; isThinking: boolean }`, `UserMeta` = `{ id: string; info: { name, avatar, color } }`. Added `lib/liveblocks.ts`: exports `liveblocks`, a `Liveblocks` (from `@liveblocks/node`) client instance, and `getCursorColor(userId)` — a `>>> 0`-masked string hash into a fixed 8-color palette (cyan/indigo/blue/purple/pink/red/orange/green, matching the vivid accent-style colors from `ui-context.md`) for deterministic per-user cursor colors. Unlike `lib/prisma.ts`'s eager module-scope singleton, the Liveblocks client is instantiated lazily behind a `Proxy` wrapping a memoized getter — `next build`'s "Collecting page data" step evaluates every route module at build time (import-time side effects, not just type collection), and `new Liveblocks({ secret })` throws synchronously if the key doesn't start with `sk_`; since this repo doesn't have a real Liveblocks secret key yet (`LIVEBLOCKS_SECRET_KEY=` was added empty to `.env.example`/`.env.local`, needs a real value from the Liveblocks dashboard before real-time features actually work end to end), eager instantiation broke `npm run build`. The lazy `Proxy` defers `new Liveblocks(...)` (and thus secret validation) until the first actual property access at request time, so build no longer depends on the secret being present. Extended `lib/project-access.ts`'s `ClerkIdentity`/`getCurrentIdentity()` with `name`/`avatarUrl` (derived from the same `currentUser()` call it already made — `fullName ?? username` and `imageUrl` — no extra Clerk fetch) since the auth route needs more than `userId`/`email`; existing callers (`collaborators` routes, `/editor/[roomId]`) are unaffected since they only destructure the fields they use. Added `app/api/liveblocks-auth/route.ts`: `POST` requires Clerk auth (401 via `getCurrentIdentity`), reads `room` (the Liveblocks room ID, sent automatically by the client SDK's default `authEndpoint` fetch) from the JSON body (`parseRoomId`, local to the route — only one caller so not promoted to `lib/`), calls `getAccessibleProject(roomId, identity)` — reusing Feature 08's owner-or-collaborator check, since the spec's "use the project ID as the Liveblocks room ID" means project access and room access are the same check — and returns `403` if it fails. On success, calls `liveblocks.getOrCreateRoom(roomId, { defaultAccesses: [], usersAccesses: { [userId]: ["room:write"] } })` (private by default; only explicitly granted users get in) and, since `getOrCreateRoom` only applies its `usersAccesses` param on first creation and doesn't retroactively grant access to a project's later-added collaborators on an already-existing room, follows up with `liveblocks.updateRoom(roomId, { usersAccesses: { [userId]: ["room:write"] } })` whenever the just-fetched room's `usersAccesses` doesn't already include this user — keeping per-user Liveblocks room permissions in sync with the Postgres-backed project/collaborator model on every auth call, without an extra write when access is already correct. Finishes with `liveblocks.identifyUser(userId, { userInfo: { name, avatar, color: getCursorColor(userId) } })` and returns its `{ status, body }` directly as the response, per the Liveblocks ID-token auth pattern. Verified via `tsc --noEmit`, `npm run lint` (clean, same pre-existing `share-dialog.tsx` `<img>` warning as Feature 09), and `npm run build` (all clean; route table shows `ƒ /api/liveblocks-auth` registered as dynamic). Not verified against a real Liveblocks room end-to-end — no real `LIVEBLOCKS_SECRET_KEY` is configured yet, and no UI calls this endpoint until Feature 11 wires up `LiveblocksProvider`/`RoomProvider` on the canvas.
- **11 — Base Canvas**: Replaced `components/canvas-placeholder.tsx` (deleted) with a real Liveblocks-backed React Flow canvas, rendered from `components/workspace-shell.tsx` as `<Canvas roomId={project.id} />` (room ID = project ID, per the Feature 07/10 alignment decision). Added `types/canvas.ts`: `NodeShape` (the 6-value union from `ui-context.md` — `rectangle`/`diamond`/`circle`/`pill`/`cylinder`/`hexagon` — typed now even though shape-specific rendering doesn't land until Features 12/13, since node data needs a real type today); `CanvasNodeData` (`label`, `color`, `shape`); `CanvasNode = Node<CanvasNodeData, "canvasNode">` and `CanvasEdge = Edge<Record<string, unknown>, "canvasEdge">` from `@xyflow/react`. Did not add a `NODE_COLORS`/`NODE_SHAPES` palette constant here — `ui-context.md` documents that data living in this file, but populating it is out of scope until the color-toolbar/shape-panel features (12/13/15/16) actually consume it; adding it now would be speculative. Added `components/canvas.tsx` (`"use client"`) — the room-setup wrapper: `LiveblocksProvider authEndpoint="/api/liveblocks-auth"` → `RoomProvider id={roomId} initialPresence={{ cursor: null, isThinking: false }}` (both `Presence` fields required, since Feature 10's `liveblocks.config.ts` declares `isThinking` as non-optional) → `ErrorBoundary` (from the newly-installed `react-error-boundary`) wrapping `ClientSideSuspense`, matching the skill-recommended pattern exactly rather than hand-rolling `useErrorListener`-based error handling, since the spec only asked for "a simple loading state" and "an error fallback" — nothing about granular per-error-code handling. `CanvasLoadingFallback`/`CanvasErrorFallback` are small local components in the same file (not promoted to their own files — single-use, trivial, no reuse candidate) styled with the same dark-theme tokens `canvas-placeholder.tsx` used. Also imports `@xyflow/react/dist/style.css` here (the canvas's actual mount point) rather than in `app/globals.css`, since it's React Flow-specific and this is the only place React Flow renders. Added `components/canvas-flow.tsx` (`"use client"`) — the inner component, only mounted once Liveblocks Storage is ready (it's the `ClientSideSuspense` child): calls `useLiveblocksFlow<CanvasNode, CanvasEdge>({ suspense: true, nodes: { initial: [] }, edges: { initial: [] } })` from `@liveblocks/react-flow` and passes the returned `nodes`/`edges`/`onNodesChange`/`onEdgesChange`/`onConnect`/`onDelete` straight into `<ReactFlow>`, plus `connectionMode={ConnectionMode.Loose}` (any handle can start/end a connection, not just source→target), `fitView`, a dotted `<Background variant={BackgroundVariant.Dots} />`, and `<MiniMap>` — no `<Controls>`, no custom node/edge `nodeTypes`/`edgeTypes` registration, and no `<Cursors />` presence overlay, all per the spec's explicit scope limits (controls, custom rendering, and presence/avatars are later features — 19 for cursors). Installed `@liveblocks/node` in Feature 10 already covered the server SDK; this feature additionally installed `react-error-boundary` (no Liveblocks package was missing — `@liveblocks/react-flow` and `@xyflow/react` were already present). Verified via `tsc --noEmit`, `npm run lint` (one new `react/no-unescaped-entities` error on the error fallback's apostrophe, fixed with `&apos;`; otherwise clean aside from the pre-existing `share-dialog.tsx` `<img>` warning), and `npm run build` (all clean; `/editor/[roomId]` still renders as `ƒ` dynamic — the canvas itself is entirely client-side within it). Not verified visually in-browser end-to-end (a real `LIVEBLOCKS_SECRET_KEY` was added to `.env.local` since Feature 10, but the route still sits behind Clerk auth — same constraint noted in Features 04/08/09) — confirmed instead that an already-running dev server on port 3000 responds and that the build's route table and `tsc`/`eslint` passes are clean.
- **12 — Shape Panel**: Extended `types/canvas.ts` (still no full `NODE_COLORS` palette — that stays deferred to the color-toolbar features, 15/16) with `ShapeDefinition` (`shape`, `label`, `defaultWidth`, `defaultHeight`), the populated `NODE_SHAPES` array (sensible default sizes per spec: rectangle 160×80, diamond 180×140 — larger so labels fit, circle 100×100 square, pill 160×56, cylinder 120×110, hexagon 170×100), and `DEFAULT_NODE_COLOR = "#1F1F1F"` (the "Neutral dark (default)" fill from `ui-context.md`'s palette table — only the fill was added, not a paired text color/full palette entry, since nodes render with a fixed `text-copy-primary` for now and per-color text pairing is color-toolbar scope). Added `lib/shape-drag.ts`: `SHAPE_DRAG_MIME_TYPE` constant, `ShapeDragPayload` (`shape`, `width`, `height`), and `serializeShapeDragPayload`/`parseShapeDragPayload` — the parser validates the shape against `NODE_SHAPES` and checks `width`/`height` are numbers before trusting the payload, per `code-standards.md`'s "validate unknown external input at system boundaries" (drag-and-drop's `DataTransfer` is exactly such a boundary — its contents cross out of React's type system). Added `components/shape-panel.tsx` (`"use client"`) — a `rounded-full` icon-button row (shadcn `Button variant="ghost" size="icon"`, each with `draggable` + `onDragStart` serializing a `ShapeDragPayload` via `lib/shape-drag.ts`) for the 6 `NODE_SHAPES`, using Lucide's `Square`/`Diamond`/`Circle`/`Pill`/`Cylinder`/`Hexagon` icons (all exist as direct Lucide exports, so no custom SVGs needed for this unit's icon buttons — the shape *nodes* themselves still render as plain bordered rectangles per scope, see below). Added `components/canvas-node.tsx` (`"use client"`) — `CanvasNodeRenderer`, the first registered `"canvasNode"` type: per spec, every shape renders identically for this unit — a `rounded-xl` bordered box (`border-surface-border`) filled with `data.color` via inline `style` (arbitrary hex from Liveblocks storage, not a static Tailwind class) and the label centered; no shape-specific SVG paths and no connection handles yet (`16-edge-behavior.md` explicitly owns adding handles to the node renderer — its scope limits say "don't redesign the node renderer beyond the required connection handles," confirming handles are intentionally deferred, not an oversight here). Extended `components/canvas-flow.tsx`: registered `nodeTypes = { canvasNode: CanvasNodeRenderer }` at module scope (stable reference — an inline object would trigger React Flow's nodeTypes-changed warning every render); added a `reactFlowInstanceRef` populated via `<ReactFlow onInit>` (needed because `useReactFlow()` can't be called from the same component that renders `<ReactFlow>` itself — it only works in descendants — so `onInit` capturing the instance directly was simpler than restructuring into a `ReactFlowProvider` + child-component split for this one call site); `onDragOver` (guarded on `dataTransfer.types.includes(SHAPE_DRAG_MIME_TYPE)`, calls `preventDefault`) and `onDrop` (reads + parses the payload, converts the drop point with `reactFlowInstance.screenToFlowPosition`, centers the new node under the cursor by subtracting half its width/height, and appends it via `onNodesChange([{ type: "add", item: newNode }])` — the Liveblocks flow hook has no separate `addNode`/`setNodes` escape hatch, so a React Flow `NodeAddChange` is the documented way to insert a node through it) wired directly onto `<ReactFlow>` (it forwards unknown props to its root div, so no extra wrapper element was needed for "the canvas wrapper" the spec asked for). New node IDs come from a local `generateNodeId(shape)` (module-scope incrementing counter + `Date.now()`, per spec's "shape name, timestamp, and a counter" — not promoted to `lib/` since this is still the only caller, matching the established promote-on-second-use convention). The toolbar itself mounts as `<Panel position="bottom-center"><ShapePanel /></Panel>` — React Flow's own `Panel` component, which positions overlay UI relative to the canvas viewport without hand-rolled absolute-positioning CSS. Verified via `tsc --noEmit`, `npm run lint` (clean aside from the pre-existing `share-dialog.tsx` `<img>` warning), and `npm run build` (all clean). Not verified visually in-browser — same Clerk-auth constraint as prior editor-route features.
- **13 — Node Shape**: Added `components/node-shape.tsx` — the new `NodeShapeVisual({ shape, color, label?, selected?, className? })`, a single shared component now used by both the real canvas node and its drag preview (see below), replacing `canvas-node.tsx`'s old one-size-fits-all bordered box. Rectangle/pill/circle render as a plain CSS div (`rounded-xl` for rectangle, `rounded-full` for pill/circle — `rounded-full` on a non-square div gives the stadium/pill shape, on a square div gives a true circle) with `border`/`backgroundColor` inline styles. Diamond/hexagon/cylinder render as inline SVG: a fixed `viewBox="0 0 100 100"` with `preserveAspectRatio="none"` so the shape stretches non-uniformly to fill whatever pixel width/height the node currently has — this was simpler than reading the node's actual measured `width`/`height` from `NodeProps` and recomputing a path, and satisfies the spec's "SVG shapes should scale with node size" without extra plumbing. Diamond is a 4-point rhombus polygon; hexagon is a 6-point polygon with flat top/bottom edges and pointed left/right vertices (fits the wider-than-tall default size from `NODE_SHAPES`); cylinder is a rounded-corner body `path` plus a separate top `ellipse` drawn on top of it (same fill, same stroke) to read as a barrel with a visible rim, the standard cylinder-icon construction. Border treatment (both CSS and SVG variants) is driven by a `selected` boolean: `var(--border-default)`/1px at rest, `var(--accent-primary)`/2px when selected — no new CSS tokens added, both already existed in `globals.css`. `components/canvas-node.tsx`'s `CanvasNodeRenderer` is now a thin wrapper: it destructures `data` and the already-available `selected` prop from `NodeProps` (React Flow provides `selected` directly, no need to track it separately) and passes them straight to `NodeShapeVisual`. **Drag preview**: `components/shape-panel.tsx` keeps its existing Lucide-icon toolbar buttons and `onDragStart` payload logic untouched, but now also renders one `NodeShapeVisual` per shape (real shape + `DEFAULT_NODE_COLOR`, sized to that shape's `defaultWidth`/`defaultHeight`) into an always-mounted off-screen container (`fixed left-[-9999px] top-[-9999px]`, not `display:none`/`opacity:0` — browsers need an element actually laid out in the DOM to rasterize it as a drag image) and calls the native `event.dataTransfer.setDragImage(previewRefs.current[shape], width/2, height/2)` in `handleDragStart`, centering the ghost on the cursor. This uses the browser's native HTML5 drag-and-drop ghost-image mechanism rather than a hand-rolled cursor-tracking overlay: the ghost automatically follows the cursor during the drag and automatically disappears on drop or cancel (`dragend`) with no extra code, which is what let this stay scoped to `shape-panel.tsx` alone — `canvas-flow.tsx`'s `onDragOver`/`onDrop` handlers were not touched. Verified via `tsc --noEmit`, `npm run lint` (clean aside from the pre-existing `share-dialog.tsx` `<img>` warning), and `npm run build` (all clean). Not verified visually in-browser — same Clerk-auth constraint as prior editor-route features; the shape/border/drag-preview logic itself has no auth dependency, so a manual visual pass is lower-risk than usual if the user wants to spot-check it.
- **14 — Node Editing**: Added `MIN_NODE_WIDTH = 60`/`MIN_NODE_HEIGHT = 40` to `types/canvas.ts` (a single global floor, not per-shape — spec just asked for "a minimum size," and per-shape minimums would've been speculative). **Resizing**: `components/canvas-node.tsx` now renders `<NodeResizer isVisible={selected} minWidth={MIN_NODE_WIDTH} minHeight={MIN_NODE_HEIGHT} handleStyle={{...}} />` (from `@xyflow/react`) as a sibling of `NodeShapeVisual` inside `CanvasNodeRenderer` — the standard "resizable custom node" pattern from React Flow's own docs. `handleStyle` overrides the library's default blue squares with small dark-filled, accent-bordered circles (`backgroundColor: var(--bg-elevated)`, `border: 1.5px solid var(--accent-primary)`) to read as subtle/native to the dark canvas rather than a generic library default; `lineStyle` (the invisible edge-drag hit-strips) was left alone since they have no visual footprint to restyle. `NodeResizer` needs no `nodeId` prop when rendered inside the custom node itself (it reads the ambient node id from context) and — critically — it updates dimensions by calling the same `triggerNodeChanges` → `onNodesChange` pipeline that node dragging and Feature 12's drag-and-drop already flow through, so resize changes reach `useLiveblocksFlow`'s sync logic with zero changes to `canvas-flow.tsx`. **Label editing**: `components/node-shape.tsx`'s `NodeShapeVisual` was restructured — shape geometry moved into a new internal `ShapeBackground` (unchanged visuals, just extracted so it renders with no label baked in) and the label became a single always-in-the-same-place absolutely-positioned overlay `div` (`absolute inset-0 flex items-center justify-center`) that swaps its *content* between a static centered `<span>` (showing the label, or muted "Double-click to add a label" placeholder text when empty) and a same-sized `<textarea>` while editing — since both occupy the identical overlay box, nothing shifts when editing starts/stops. Added a `showLabel` prop (default `true`) so `components/shape-panel.tsx`'s off-screen drag-preview instances (Feature 13) can opt out of the label/placeholder overlay entirely — they call `NodeShapeVisual` with `label=""`, which would otherwise show the "Double-click to add a label" placeholder text baked into the drag ghost image. `CanvasNodeRenderer` owns the actual editing state (`isEditing`, local `useState`) and handlers: double-click on the label area calls `event.stopPropagation()` (prevents the double-click from also bubbling to React Flow's pane and triggering its default `zoomOnDoubleClick` zoom-in) then opens the textarea; every keystroke (`onChange`) calls `updateNodeData(id, { label: value })` from `useReactFlow()` — this is the documented, officially-supported way to patch a controlled node's `data` from within its own custom-node component (it diffs against the current controlled `nodes` array and calls the same `onNodesChange` callback wired to `useLiveblocksFlow`, exactly like `NodeResizer` above — confirmed by reading `@xyflow/react`'s source, since neither `useLiveblocksFlow` nor `NodeProps` expose a more direct node-data-update API); blur or `Escape` (`Escape` just calls `.blur()` on the textarea) closes editing by flipping `isEditing` back to `false` — no separate "commit" step since the label is already synced live on every keystroke. The textarea carries React Flow's `nodrag nopan` classes (documented convention for interactive elements inside custom nodes) so typing, selecting text, or clicking inside it never starts a node drag or canvas pan. Verified via `tsc --noEmit`, `npm run lint` (clean aside from the pre-existing `share-dialog.tsx` `<img>` warning), and `npm run build` (all clean). Not verified visually in-browser — same Clerk-auth constraint as prior editor-route features.

- **15 — Node Color Toolbar**: Added `NODE_COLORS: NodeColorOption[]` (`{ label, background, text }`, the 8 predefined pairs straight from `ui-context.md`'s Node Color Palette table) and `DEFAULT_NODE_TEXT_COLOR = "#EDEDED"` to `types/canvas.ts` — none of the 8 hex pairs overlap with `globals.css`'s UI tokens (checked per spec step 1), so per `ui-context.md`'s own instruction they stay local to canvas data rather than becoming new CSS variables. `CanvasNodeData` (`types/canvas.ts`) gained a required `textColor: string` field alongside the existing `color` field — a color pair is background+text together, and storing both explicitly (rather than deriving text color from background via a `NODE_COLORS` lookup at render time) keeps `NodeShapeVisual` a pure prop-renderer with no palette-lookup logic, and stays correct even if a node's `color` were ever set outside the predefined palette. `components/canvas-flow.tsx`'s drop-created node data now sets `textColor: DEFAULT_NODE_TEXT_COLOR` alongside `color: DEFAULT_NODE_COLOR`. Added `components/node-color-toolbar.tsx` — `NodeColorToolbar`, built on `@xyflow/react`'s own `<NodeToolbar>` (the library's dedicated "float UI above/below a node" primitive, same "use the library's built-in mechanism" approach as `Panel` for the shape panel and `NodeResizer` for resize handles) with `position={Position.Top}` and `offset={12}` so it sits just clear of the node without overlapping it, and `isVisible` driven explicitly by the node's `selected` prop rather than relying on `NodeToolbar`'s undocumented-in-code default-visibility behavior. Renders one circular swatch per `NODE_COLORS` entry; active state is a solid 2px ring in the swatch's own paired text color (`shadow-[0_0_0_2px_var(--swatch-glow)]`, `--swatch-glow` set as an inline custom property since the color is per-swatch data, not a static token) and hover state is a small soft glow in the same color (`shadow-[0_0_4px_1px_var(--swatch-glow)]`, deliberately small blur/spread so it reads as "subtle glow" rather than a diffuse halo) — both driven by plain CSS `:hover`/conditional classes, no JS mouse handlers. The toolbar's root `<NodeToolbar>` carries `nodrag nopan` (same convention as the label textarea from Feature 14) so clicking a swatch never starts a node drag or canvas pan. `components/canvas-node.tsx` added `handleColorSelect(background, text)` → `updateNodeData(id, { color: background, textColor: text })` — the same `useReactFlow().updateNodeData` sync path documented in Feature 14, so swatch clicks reach `useLiveblocksFlow` with no new plumbing — and renders `<NodeColorToolbar isVisible={selected} activeColor={data.color} onSelect={handleColorSelect} />` as a new sibling alongside the existing `NodeResizer`/`NodeShapeVisual`. `components/node-shape.tsx`'s label `<span>`/`<textarea>` switched from the static `text-copy-primary` Tailwind class to an inline `style={{ color: textColor }}` (new optional `textColor` prop, default `"var(--text-primary)"` for callers that don't pass one) — same reasoning as `color`/background already being inline-styled: arbitrary per-node hex can't be expressed as a static Tailwind token. Note: `components/node-shape.tsx` was hand-edited outside this session's control between Feature 14 and this feature — the label placeholder text ("Double-click to add a label") shown when a node's label is empty was removed, so empty-label nodes now render no placeholder at all (just blank space) instead of the muted hint Feature 14 added; this was left as-is (not restored) since it was an intentional external edit, not a regression from this feature's changes. Verified via `tsc --noEmit`, `npm run lint` (clean aside from the pre-existing `share-dialog.tsx` `<img>` warning), and `npm run build` (all clean). Not verified visually in-browser — same Clerk-auth constraint as prior editor-route features.

- **16 — Edge Behavior**: Resolved the user-reported "can't connect two shapes with a line" bug — nodes previously had zero `<Handle>` elements, so there was nothing to drag a connection from. **Connection handles**: `components/canvas-node.tsx` now renders one `<Handle type="source" position={...}>` per side (`Position.Top/Right/Bottom/Left`, each with a matching `id` so React Flow can tell them apart) via a small `HANDLE_POSITIONS` array. All four are `type="source"` rather than a source+target pair per side — confirmed by reading `@xyflow/react`'s `HandleComponent` source that a handle's connection-drag listener (`onPointerDown`) is wired regardless of its `type`, and completion-side type-matching is only enforced under `ConnectionMode.Strict`; since `canvas-flow.tsx` already sets `ConnectionMode.Loose` (Feature 11), a single handle per side is sufficient for "connect from any handle to any other handle," avoiding 8 stacked/overlapping handle elements per node. Handles are styled small-white/dark-border via inline `style` (`background: var(--text-primary)`, `border: 1.5px solid var(--bg-base)`) but hidden-until-hover via a new global CSS rule in `app/globals.css` (`.react-flow__node .arch-flow-handle { opacity: 0 }` / `:hover { opacity: 1 }`) rather than a Tailwind `group-hover` class — the hover target needed is React Flow's own `.react-flow__node` wrapper div, which this component doesn't render itself (it's injected by the library one level up), so a plain CSS descendant/`:hover` rule was the only way to react to hovering the *whole node* without restructuring the component's render tree or risking `NodeResizer`'s absolute-positioning basis. This is the one node-renderer change the spec's scope limits allow ("don't redesign the node renderer beyond the required connection handles"). **Edge type + default styling**: `types/canvas.ts` gained `CanvasEdgeData` (`{ label?: string }`, replacing the previous `Record<string, unknown>` edge data type), `CanvasEdge = Edge<CanvasEdgeData, "canvasEdge">`, and `DEFAULT_EDGE_COLOR = "#f8fafc"` (ui-context.md's literal edge color — kept local to canvas data rather than a `globals.css` token, same reasoning already established for `NODE_COLORS` in Feature 15). `components/canvas-flow.tsx` registers `edgeTypes = { canvasEdge: CanvasEdgeRenderer }` (same stable-module-scope-reference convention as `nodeTypes`) and a `defaultEdgeOptions` object (`{ type: "canvasEdge", markerEnd: { type: MarkerType.ArrowClosed, color: DEFAULT_EDGE_COLOR, width: 18, height: 18 } }`) passed to `<ReactFlow defaultEdgeOptions={...}>` — confirmed by reading `HandleComponent`'s `onConnectExtended` that React Flow merges `defaultEdgeOptions` into every new connection's params before calling `onConnect`, so this is sufficient to make every handle-drawn connection use the custom edge type and get an arrowhead with zero changes to `onConnect` itself (still passed straight through from `useLiveblocksFlow`). **Custom edge renderer**: Added `components/canvas-edge.tsx` (`CanvasEdgeRenderer`) — deliberately does *not* use the library's `<BaseEdge>` helper, even though it's the documented shortcut for "wide invisible interaction path + visible path": `BaseEdge` only forwards passed-through props (`onMouseEnter`, `onDoubleClick`, etc.) onto the *visible* thin path, not its auto-generated wide interaction path (confirmed by reading `BaseEdge`'s source), so hover/double-click detection would only work over the thin 1.5px line — not "easier to hover and click" as the spec requires. Instead renders two manual `<path>`s sharing one `getSmoothStepPath({ ..., borderRadius: 0 })` computation (`borderRadius: 0` = sharp right-angle corners = the spec's "clean right-angle routing," while still using the `getSmoothStepPath` family so its returned `labelX`/`labelY` midpoint stays available for the label, per spec's explicit "do not calculate midpoint position manually"): a wide (`strokeWidth: 20`) transparent path carrying `onMouseEnter`/`onMouseLeave`/`onDoubleClick` (local `isHovered`/`isEditing` state, `nodrag nopan` classes) as the real interactive surface, and a thin (`strokeWidth: 1.5`, `strokeLinecap: "round"`) visible path with `pointerEvents: "none"` (so it never intercepts anything, letting the wide path handle all interaction) whose `opacity` toggles between a dimmed rest state (`0.55`) and full brightness when `selected || isHovered || isEditing` — satisfying "dimmed at rest, brighten on hover/selected" without any visual thickness change. **Label editing**: an `<EdgeLabelRenderer>` portal renders an HTML overlay positioned at the path's `[labelX, labelY]` (`translate(-50%,-50%) translate(Xpx,Ypx)`, the documented centering pattern for that API) with three states — a saved label renders as a small pill badge (`rounded-full border px-2.5 py-0.5 text-xs`); an empty label shows a dashed-border "Add label" hint pill, but *only* when `isActive` (selected/hovered/editing), so edges stay visually clean at rest per spec's "when an *active* edge has no label, show a faint hint"; double-clicking either state (`stopPropagation`, same zoom-prevention reasoning as node label editing in Feature 14) opens an `<input>` whose `width` is set via `${length + 1}ch` inline style so it grows with typed content without a hidden-measurement-span hack. Committing goes through `updateEdgeData(id, { label })` from `useReactFlow()` — the edge-side sibling of `updateNodeData` (Features 14/15), same controlled-`onEdgesChange` sync pipeline. Unlike the node label editor (which synced live per-keystroke and let blur/Escape just close), the edge input keeps the draft in local `useState` and commits explicitly in `onBlur`/on `Enter`-or-`Escape`-triggered blur — matching the spec's literal wording that Escape *saves* here, not discards, which is the opposite of the usual escape-to-cancel convention and was easy to get backwards. Verified via `tsc --noEmit`, `npm run lint` (clean aside from the pre-existing `share-dialog.tsx` `<img>` warning), and `npm run build` (all clean). Not verified visually in-browser — same Clerk-auth constraint as prior editor-route features; this is a good candidate for the user to spot-check given it's the feature that unblocks a previously-reported broken interaction.

## In Progress

- None.

## Next Up

- Feature 17: Canvas Ergonomics (`context/features/17-canvas-ergonomics.md.md`).

## Open Questions

- None currently.

## Architecture Decisions

- Theme is dark-only: both `:root` and `.dark` in `globals.css` hold identical dark values, and `<html>` always carries the `dark` class — this satisfies the `dark:` variant classes baked into generated `components/ui/*` files without needing a toggle.
- shadcn `radix-nova` style/preset chosen (Lucide icons, Geist font) to match `ui-context.md`'s icon and typography requirements.
- `--radius` set to `0.75rem` so button/input corners land near the `rounded-xl` scale from `ui-context.md`; note that Card and Dialog hardcode literal `rounded-xl` classes in the generated components rather than deriving from `--radius`, so they don't reach `rounded-2xl`/`rounded-3xl` — accepted as-is since `components/ui/*` must not be modified.

## Session Notes

- `components/ui/*` must not be edited (per `AGENTS.md` and `ai-workflow-rules.md`); any future project-specific styling goes in app-level components instead.
- To add more shadcn components later, run `npx shadcn@latest add <name> -y`.
- New convention from Feature 04: dialog/form/loading state that multiple components need to share (e.g. sidebar rows + a home-screen button all opening the same dialog) lives in a `hooks/` hook, not in `components/`, and the nearest shared client component owns the single hook instance and passes data/callbacks down as props.
- Superseded by Feature 07: `app/editor/page.tsx` is no longer the state-owning client component — it went back to being a Server Component so it could fetch project data server-side. `components/editor-shell.tsx` is now that owner instead (a `"use client"` component the page renders with `ownedProjects`/`sharedProjects` as props). General rule going forward: whenever a route needs both a server-side data fetch *and* client state/hooks, split it into an `async` Server Component page plus a sibling `"use client"` shell component that the page renders — don't put both concerns in one file.
- New from Feature 07: `lib/` now holds `lib/projects.ts` (`getOwnedAndSharedProjects(userId, email)`, used by the `/editor` Server Component) alongside the existing `lib/project-api.ts` (route-handler helpers) and `lib/prisma.ts` — the split is "server-component-facing data helpers" vs. "route-handler request/response helpers," both under `lib/` per `architecture-context.md`. `POST /api/projects` now accepts an optional client-supplied `id` (validated by `parseOptionalProjectId` in `lib/project-api.ts`) so a project's database ID can be set up front to match the Liveblocks room ID it'll use once Feature 10 lands — Feature 08's `/editor/[roomId]` route and Feature 10's "use the project ID as the Liveblocks room ID" both assume this alignment holds from creation time.
- Clerk's SDK logs a deprecation warning for `createRouteMatcher`/middleware-based route protection, recommending per-page resource-based `auth()` checks instead. `proxy.ts` still uses `createRouteMatcher` because `03-auth.md` explicitly specifies "protect everything else by default" at the proxy layer — kept as-is since it's a deprecation warning, not a removal, and matches the spec.
- New from Feature 05: import Prisma's generated client as `@/app/generated/prisma/client` (not `@/app/generated/prisma`) — the `"prisma-client"` generator's output has no `index.ts`/`package.json`, only `client.ts` as the real entry point. Always use `prisma` from `lib/prisma.ts` in app code; never instantiate `PrismaClient` directly elsewhere. `prisma.config.ts` now explicitly loads both `.env` and `.env.local` (latter wins) via `dotenv`'s `config()` — needed because the CLI's original `import "dotenv/config"` only reads `.env`, while this repo keeps real secrets (`DATABASE_URL`, Clerk keys) in `.env.local`; if `prisma migrate`/`generate` ever reports a missing env var again, check that file first before assuming the variable itself is unset.
- New from Feature 06: every `app/api/*` route handler must call `await auth()` and explicitly return a JSON `401` itself — do not rely on `proxy.ts`'s `clerkMiddleware`/`auth.protect()` to produce a fetch-friendly `401`, since that middleware is tuned for page navigation (redirects) and isn't guaranteed to return a plain JSON 401 body for API callers. Error responses use a single shared `errorResponse(status: number, message: string)` helper (not one function per status code — user explicitly asked for this consolidated form) plus a `parseProjectName` body-parsing helper, both in `lib/project-api.ts` — extend this file (or add a sibling `lib/<resource>-api.ts`) rather than re-inlining ad hoc `NextResponse.json({ error }, { status })` calls in future route handlers, so error shapes stay consistent across resources. Ownership checks (404 if resource missing, 403 if `ownerId !== userId`) are implemented as a small local helper co-located in the route file when only that file's handlers need it (see `findOwnedProject` in `app/api/projects/[projectId]/route.ts`) rather than promoted to `lib/` until a second route needs the same check.
- New from Feature 08: `access-denied.tsx`, `workspace-shell.tsx`, `canvas-placeholder.tsx`, and `ai-sidebar-placeholder.tsx` live flat in `components/` (an initial `components/editor/` subfolder was flattened back out to match this repo's existing convention — everything stays flat in `components/`, no per-route subfolders). `lib/project-access.ts` is the "is this identity allowed to see this specific project" helper — distinct from `lib/projects.ts`'s `getOwnedAndSharedProjects` (list-for-sidebar); both are used together by `/editor/[roomId]`. `components/editor-navbar.tsx` grew optional `projectName`/`showWorkspaceActions`/`isAiSidebarOpen`/`onToggleAiSidebar` props rather than forking into a second navbar component. `components/sidebar.tsx` grew an optional `activeProjectId` prop for the current-room highlight — keep passing it through if `ProjectSidebar` is extended further.
- New from Feature 09: `lib/project-access.ts` also holds `requireProjectOwner` now (in addition to `getCurrentIdentity`/`hasProjectAccess`/`getAccessibleProject`) — it's the shared "auth + ownership" boundary for `app/api/*` routes, one level up from `lib/project-api.ts`'s pure request/response helpers (`errorResponse`, body parsers). `eslint-plugin-react-hooks`'s `set-state-in-effect` rule (shipped by default in `eslint-config-next` 16) is strict: it flags *any* synchronous `setState` call inside a `useEffect` body, not just ones that also do async work. Do not use a fetch-on-mount/fetch-on-open `useEffect` pattern in this codebase — instead give the owning hook an explicit `open()`/`load()` function called directly from the triggering event handler (e.g. a button's `onClick`), same as `use-project-actions.ts` already does for its dialogs. `components/editor-navbar.tsx`'s Share button now has a real `onShare` prop/`onClick` — Feature 08's placeholder note about "wire it there" is resolved.
- New from Feature 10: **`LIVEBLOCKS_SECRET_KEY` is not yet set to a real value** (`.env.local`/`.env.example` have it declared but empty) — get one from the Liveblocks dashboard (https://liveblocks.io/dashboard/apikeys) before Feature 11's canvas can actually connect to a room; `POST /api/liveblocks-auth` will 500 until then. Server-side Liveblocks clients (`Liveblocks` from `@liveblocks/node`, see `lib/liveblocks.ts`) must be instantiated lazily (behind a `Proxy`/memoized getter), not eagerly at module scope like `lib/prisma.ts` — `next build`'s "Collecting page data" step imports every route module, and `new Liveblocks({ secret })` throws synchronously on an invalid/empty key, which breaks `npm run build` even though the route is never actually invoked at build time. `lib/project-access.ts`'s `ClerkIdentity`/`getCurrentIdentity()` now also carries `name`/`avatarUrl` (sourced from the same `currentUser()` call, no extra fetch) — reuse these instead of calling `currentUser()` again when a route needs display info beyond `userId`/`email`.
- New from Feature 12: `types/canvas.ts` now holds `NODE_SHAPES` (shape metadata + default sizes) and `DEFAULT_NODE_COLOR` — still no full `NODE_COLORS` fill/text-pair palette, that stays reserved for the color-toolbar features (15/16). To add a node to the Liveblocks-synced flow, dispatch `onNodesChange([{ type: "add", item: newNode }])` (a React Flow `NodeAddChange`) — `useLiveblocksFlow` exposes no separate `addNode`/`setNodes` method. `useReactFlow()`/`screenToFlowPosition` can't be called from the component that renders `<ReactFlow>` itself (only from its descendants); capture the instance via `<ReactFlow onInit={(instance) => ...}>` into a ref instead when you need imperative access from the same component. `<ReactFlow>` forwards unrecognized props to its root div, so drag/drop handlers (`onDragOver`/`onDrop`) and other native div events can be passed directly as props rather than adding a wrapper element. Floating in-canvas UI (the shape panel, and presumably future canvas toolbars) mounts via React Flow's own `<Panel position="...">`, not hand-rolled absolute positioning. `components/canvas-node.tsx` (`CanvasNodeRenderer`) is the one registered `"canvasNode"` type in `nodeTypes` — extend it in place for Feature 13's shape-specific rendering rather than creating per-shape components, and remember `nodeTypes` must stay a stable module-scope reference (not redefined inline in render). Drag payloads crossing the browser's native `DataTransfer` boundary are untrusted input — `lib/shape-drag.ts`'s `parseShapeDragPayload` is the validation pattern to extend if the payload shape grows (e.g. Feature 18's starter templates dragging in prebuilt node clusters).
- New from Feature 13: `components/node-shape.tsx`'s `NodeShapeVisual` is now the single place shape geometry is drawn — reuse it (don't fork per-shape rendering again) if a future feature needs the same shapes elsewhere (e.g. a template preview thumbnail). `NodeProps` from `@xyflow/react` already exposes `selected` (and `width`/`height`) directly — no need to track selection state separately for node-level styling. SVG shapes use a fixed `viewBox="0 0 100 100"` + `preserveAspectRatio="none"` to scale with node size, rather than reading the node's actual pixel dimensions — reuse this pattern (not a width/height-driven path recompute) if more SVG shapes are added later. For a native HTML5 drag-and-drop ghost image that isn't the browser's default element snapshot, render the desired preview element for real (off-screen via `fixed left-[-9999px]`, never `display:none`/`opacity:0` — those produce a blank drag image) and call `event.dataTransfer.setDragImage(el, offsetX, offsetY)` in `onDragStart`; the browser handles cursor-following and cleanup on drop/cancel automatically, no manual mousemove tracking or dragend cleanup needed.
- New from Feature 14: to mutate a single controlled node's `data` (or any field) from *inside* that node's own custom-node component, call `updateNodeData(id, partialData)` (or `updateNode`) from `useReactFlow()` — it diffs against the live controlled `nodes` array and invokes the same `onNodesChange` callback already wired to `<ReactFlow onNodesChange={onNodesChange}>`, so it reaches `useLiveblocksFlow`'s sync logic with no new plumbing. `NodeResizer` (also from `@xyflow/react`) works the same way for dimension changes — both go through `triggerNodeChanges` → the app's `onNodesChange`, confirmed by reading the library source since this isn't obvious from the type signatures alone. Neither `useLiveblocksFlow` nor `NodeProps` expose a more direct "update this node" API — `useReactFlow()`'s hooks are the intended escape hatch, callable from any descendant of `<ReactFlow>` (including custom node components themselves, not just siblings). `components/node-shape.tsx`'s label is now a single overlay `div` whose *contents* swap between static text and a `<textarea>` (both filling the identical box) — this "same box, swapped contents" pattern is how "no layout shift while editing" was satisfied, and is the pattern to reuse for any future in-place-editable node content. Interactive elements inside a custom node (inputs, textareas) need both the `nodrag` and `nopan` classes to stop React Flow from starting a node drag or canvas pan on interaction with them. A custom node's own `onDoubleClick` handler should call `event.stopPropagation()` if it shouldn't also trigger the pane's default `zoomOnDoubleClick` zoom (double-click events bubble from node content up through the pane).
- New from Feature 15: `types/canvas.ts`'s `NODE_COLORS` (8 background/text pairs) is now the canonical node color palette — reuse it for any future color-related UI (e.g. a legend, an export/spec view) rather than re-deriving pairs elsewhere. `CanvasNodeData` now carries both `color` (background) and `textColor` explicitly; anywhere a node is created (currently only `canvas-flow.tsx`'s drop handler) must set both together, and any future bulk-mutation/import path must do the same. For floating UI anchored to a node (as opposed to the canvas viewport, which uses `Panel`), use `@xyflow/react`'s `<NodeToolbar>` — it auto-positions above/below/beside the node and only needs `isVisible`/`position`/`offset`, no manual absolute-positioning math. Dynamic per-item hover/active colors that can't be static Tailwind classes (like each color swatch's own glow color) can be passed as inline CSS custom properties (`style={{ "--foo": value }}`, cast `as CSSProperties`) and referenced from Tailwind arbitrary-value utilities (`hover:shadow-[...var(--foo)]`) — this keeps the interaction state in pure CSS instead of JS mouse handlers. **Heads up**: `components/node-shape.tsx` was edited outside of a tracked feature session between Features 14 and 15 — the empty-label placeholder text Feature 14 added ("Double-click to add a label") is gone; empty labels now render nothing. If a future feature needs that hint back, it'll need to be re-added deliberately, not assumed present.
- New from Feature 16: with `connectionMode={ConnectionMode.Loose}` (set since Feature 11), a node only needs **one** `<Handle>` per side (not a stacked source+target pair) to support "connect from any handle to any other handle" — `HandleComponent`'s connection-start listener isn't gated by `type`, only completion-side type-matching is, and only under `Strict` mode. When you need a hover state on React Flow's own node/edge *wrapper* elements (`.react-flow__node`, `.react-flow__edge`) that your custom component doesn't render itself, a plain CSS rule in `globals.css` (`.react-flow__node:hover .my-class { ... }`) is the way to react to it — there's no prop React Flow forwards for "is this node hovered" into custom node components. `<BaseEdge>` is *not* a good fit when a custom edge needs hover/double-click detection over its full clickable width: it only forwards passed props onto the thin visible path, not the wide auto-generated interaction path, so build both paths manually if interaction and visuals need to be decoupled (wide transparent interactive path + thin `pointerEvents: none` visible path, both sharing one `getSmoothStepPath`/`getBezierPath`/etc. call). `getSmoothStepPath({ borderRadius: 0 })` is how to get sharp right-angle routing while keeping the same "family" of path util (and its `labelX`/`labelY` return values) as the rounded version. The edge-data equivalent of `updateNodeData` is `updateEdgeData(id, partialData)`, also from `useReactFlow()`, same controlled-`onEdgesChange` sync path. `EdgeLabelRenderer` content is portaled outside the SVG edge `<g>` entirely — it can't be reached by CSS selectors scoped to `.react-flow__edge`, so any hover-driven styling on an edge label must come from the same React state (not CSS) that drives the SVG path's own hover styling.
